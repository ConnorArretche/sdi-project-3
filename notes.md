Users — id, name, email

Accounts — id, acct#, account_type

User_Accounts — user_id(FK), account_id(FK)

Categories — id, name

Transactions — id, amount, description, timestamp, account_id(FK), category_id(FK)



TO-DO:

Backend:
    Cookies for themes
    404 for unknown routes



